#include "BufferConsumer.h"

//template<typename TEXTURE_ARRAY_TYPE, typename MESH_TYPE, typename TRANSFORM_TYPE>
//bool BufferConsumer<TEXTURE_ARRAY_TYPE, MESH_TYPE, TRANSFORM_TYPE>::AllocateStorage(MeshDataSlots& out_slots,
//  unsigned long vertices_size,
//  unsigned long indices_size,
//  unsigned long num_instances) {
//  return buffer->AllocateStorage(out_slots, vertices_size, indices_size);
//}
//template<typename TEXTURE_ARRAY_TYPE, typename MESH_TYPE, typename TRANSFORM_TYPE>
//void BufferConsumer<TEXTURE_ARRAY_TYPE, MESH_TYPE, TRANSFORM_TYPE>::Init()
//{
//  buffer->AddUsedBuffers(used_buffers);
//}
//template<typename TEXTURE_ARRAY_TYPE, typename MESH_TYPE, typename TRANSFORM_TYPE>
//void BufferConsumer<TEXTURE_ARRAY_TYPE, MESH_TYPE, TRANSFORM_TYPE>::PreDraw()
//{
//}
//template<typename TEXTURE_ARRAY_TYPE, typename MESH_TYPE, typename TRANSFORM_TYPE>
//void BufferConsumer<TEXTURE_ARRAY_TYPE, MESH_TYPE, TRANSFORM_TYPE>::PostDraw()
//{
//}
